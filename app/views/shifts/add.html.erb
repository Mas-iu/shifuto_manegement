<div style="text-align: center">
  <h1>出勤条件登録</h1>
  <div>
    <h3>自分が働ける、曜日と時間を登録しておきましょう</h3>
    <p>下記の選択をした後、登録ボタンを押してください。</p>
    <p>※登録ボタンを押さないと、出勤条件が未登録となります</p>
  


        <h2>
        <%#= form_with url: shifts_path do |form| 
          <div class="field">
            <%= form.label :employee_work_time_start, "出勤開始時間" 
            <%= form.datetime_field :employee_work_time_start %>
          </div>

          
          <div class="field">
            <%#= form.label :employee_work_time_end, "出勤終了時間" %>
            <%#= form.datetime_field :employee_work_time_end %>
          </div>

          <div class="actions">
            <%#= form.submit %>
          </div>
        <% #end %>
      <h3>
        <% current_date = Time.zone.now.strftime("%Y-%m") %>
        <%= form_with url: shifts_path, local: true do |form| %>
          <div class="field">
            <%= form.label :employee_work_time_start, "出勤開始時間" %>
            <%= form.datetime_local_field :employee_work_time_start, step: 1800, min: "#{current_date}T09:00", max: "#{current_date}T23:00" %>
          </div>

          <div class="field">
            <%= form.label :employee_work_time_end, "出勤終了時間" %>
            <%= form.datetime_local_field :employee_work_time_end, step: 1800, min: "#{current_date}T09:00", max: "#{current_date}T23:00" %>
          </div>

          <div class="actions">
            <%#= form.submit %>
            <%= submit_tag '登録' %>
          </div>
        <% end %>
        </h3>
  </div>
</div>



<p>☆<%= link_to("ホームへ戻る", "/shifts") %>☆</p>
<p>☆<%= link_to("シフト登録確認はこちら", "/shifts/mypage")%>☆</p>

</h2>


